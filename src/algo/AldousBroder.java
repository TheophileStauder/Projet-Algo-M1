package algo;

import graphe.Graph;

public class AldousBroder {
    private Graph graph;

    /**
     * Algorithme de AldousBroder
     *
     * On va effectuer une marche aléatoire sur le graphe en partant d'un
     * sommet quelconque , à chaque sommet non visité on garde l'arête utilisée
     * pour l'ajouter à l'abre couvrant
     */
    public AldousBroder(Graph g){
        this.graph = g;
    }

    public Graph calcul(){
        


        return new Graph(0);
    }
}
